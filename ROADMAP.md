# 🗺️ Дорожная карта Красава!

> **Файл ведёт только команда проекта.** Внешние контрибьюторы не меняют его
> напрямую — предложения по фичам оформляются через
> [Issue → Feature request](https://github.com/Vibe-Moments-Technologies/universal-schedule-app/issues/new?template=feature_request.yml).
>
> Правила ведения:
> - Задача считается выполненной только когда отмечен **весь** чеклист её DoD.
> - Выполненное не удаляем: в плане отмечаем `[x]` с пометкой версии/тега,
>   а сводка уходит в `✅ Done`.
> - Новая крупная идея сначала попадает в `🧊 Backlog`, а не сразу в план релиза.
> - Версии — по [CalVer `YY.X.Z`](.agents/VERSIONING.md), текущая линия `26.3.0`.

**Легенда:** `[ ]` в плане · `[x]` сделано · `[~]` в работе

---

## 🎯 План на 26.4.0

### R3. Поддержка ДОП-пар (дополнительные занятия) — ✅ сделано, уехало в 26.3.0

**Проблема.** В фиде `X-META-LESSON_TYPE` у доп. занятий несёт `ДОП`
(«Дополнительные занятия», например Военная кафедра). Парсер не знает этот
тип → пары падают в `OTHER` и показываются как «Задание». Некоторые студенты
ходят на них, некоторые нет — нужна возможность скрыть.

- [x] `LessonType.ADDITIONAL("Доп. занятие", "ДОП")` — бейдж в карточке
      **«ДОП»**, как в официальном расписании
- [x] Парсер: `rawType.contains("доп")` → `ADDITIONAL` (ветка ДО «пр»,
      иначе «доп. практика» уезжает в PRACTICE)
- [x] Настройка `hideAdditionalLessons` (**по умолчанию выключена** — пары видны)
      в `ScheduleStorage` + тумблер в секции настроек расписания
- [x] Фильтрация единой точкой в `ScheduleRepository` (`cachedLessons` +
      `currentLessons`) — покрыты расписание, календарные точки и `Compare`
- [x] Скрытые ДОП-пары **не планируются** в напоминания (`NotificationsManager`)
- [x] Обновление ленты/ченджлога — 26.3.0

**DoD:** ДОП-пара показывает бейдж «ДОП»; тумблер скрывает их в расписании
и глушит напоминания о них; без тумблера ничего не меняется.

---

### R2. Заметки к парам и предметам — ✅ сделано, уехало в 26.3.0

**Проблема.** Нужно записать «принести отчёт на завтрашнюю лабораторную» или
«по этому предмету всегда брать тетрадь», не теряя связь с расписанием.
Текущий `NotesScreen` — отдельный блокнот, к парам не привязан.

**Решение.** Две связанные заметки, обе хранятся на устройстве.

- [x] Заметка к **конкретной паре**, ключ: `targetId + date + bellNumber`
- [x] Заметка к **предмету в рамках расписания**, ключ: `targetId + subject`
      (нормализованный: `trim().lowercase()`)
- [x] Хранилище: `LessonNotesStorage` (отдельный класс, JSON в `PlatformStorage`)
- [x] UI в `LessonCard.kt`: блок заметки под карточкой пары,
      ≤ 2 строк с `…` на переполнении
- [x] UI в `LessonDetailScreen.kt`: панель инструментов с «Заметка к этой паре»
      и «Заметка к предмету» + кнопка «Добавить в задачи»
- [x] Каскадное удаление: `ScheduleRepository.removeTarget` →
      `lessonNotesStorage.removeNotesForTarget`
- [x] Ручная очистка в `DataAndCacheScreen`: размер заметок + кнопка
      «Очистить заметки к прошедшим занятиям» (90 дней)
- [x] Аналитика: `feature_note_added` / `feature_note_removed` + `scope`,
      **без текста** — приватность как в остальных событиях

**Явно НЕ в этой версии** (осознанно, чтобы не раздувать модель данных):
заметки к преподавателю, к аудитории, глобальные заметки к предмету между
разными сохранёнными расписаниями, автоудаление по таймеру.

**DoD:** заметка переживает рестарт; удаляется вместе с расписанием; на карточке
пары видна в свёрнутом виде; ни один тест на приватность аналитики не сломан.

---

## 🧊 Backlog

### B1. Виджеты Android и iOS

**Почему не в 26.3.0:** это две независимые нативные поставки (Glance/`AppWidget`
и WidgetKit + App Group), каждая со своей механикой обновления timeline и
отдельным тестированием на устройстве. Делать «единый KMP-API виджетов» заранее —
преждевременная абстракция.

- [ ] Общий контракт данных в `shared` (ближайшая/текущая пара), без UI
- [ ] **Android first:** Glance-виджет «Сейчас / следующая пара»
- [ ] Обновление виджета после `refreshSchedule` и при смене активного расписания
- [ ] iOS: WidgetKit + App Group container для доступа к данным из extension
- [ ] Таймлайн iOS: перезапуск в моменты `lessonStart` / `lessonEnd`
- [ ] Только после этого — вариант «короткое расписание дня» (2–4 пары)

### B2. Расширение заметок

- [ ] Заметки к преподавателю и к аудитории (нужна ясная семантика:
      преподаватель встречается в разных расписаниях, аудитория — в разных парах)
- [ ] Настройка автоочистки заметок к прошедшим парам (90 дней / семестр),
      **выключена по умолчанию** — тихо удалять пользовательские данные нельзя


### B3. Расширение просмотра расписаний

- [ ] Возможность просмотра количества занятий на 2 недели и на 4 недели от текущей даты, используя свайп вниз. 
- [ ] Настройка, которая позволяет пользователю задать количество недель в расписании по умолчанию

### B5. Remote Config — расширение (идея)

Сейчас `config.json` на gh-pages управляет только ссылками соцсетей.
Потенциальные кандидаты на удалённое конфигурирование:

- [ ] Ресурсы университета (ссылки ЛКС, СДО, Пульс, Nextcloud и т.д.)
- [ ] Команда проекта (имена, роли, e-mail, GitHub-логины)
- [ ] Контакты (почта, Issue-ссылка)
- [ ] Текст дисклеймера карт
- [ ] Ссылки на документы (TERMS, PRIVACY, PDP_POLICY)

**Почему не Remote Config RuStore/VK:** это про A/B-тесты и флаги фич.
Для ссылок/контактов — оверкилл + зависимость от чужой инфраструктуры.
Git + gh-pages даёт полный контроль, версионирование и мгновенный деплой.

### B4. Недоиспользованные данные iCal-фида

Фид (`includeMeta=true`) отдаёт больше, чем сейчас парсит `MireaICalParser`.
Маркеры недель уже взяты (источник истины для нумерации, `SemesterWeeks`);
остальное — потенциальные фичи, каждая независима:

- [ ] «N из M недель» в шапке расписания — маркеры недель дают и начало, и конец
      семестра (`X-SV-START`/`X-SV-END` в блоке `X-SCHEDULE-VERSION` — фолбэк границ)
- [ ] Полный тип занятия из `X-META-FULL_LESSON_TYPE` («Практические занятия»
      вместо «ПР») в `LessonDetailScreen`
- [ ] Кнопка «Открыть на карте» в карточке пары: `X-META-AUDITORIUM` несёт
      параметрами `NUMBER=446;CAMPUS=С-20` — надёжнее парсить их, чем `LOCATION`
- [ ] Переход к расписанию преподавателя/аудитории одним тапом из карточки пары:
      в мета-полях уже есть `ID`+`TYPE` (`X-META-TEACHER;ID=707;TYPE=2`)
- [ ] Явный признак «пара раз в две недели»: `RRULE INTERVAL=2` сейчас
      используется только для разворачивания дат
- [ ] Индикация отменённых занятий: `EXDATE` с несколькими датами
      (сейчас просто фильтруется)

---

## ✅ Done

- [x] **26.3.0 — «Красава!»** ✅
      - [x] Ребрендинг: имя «Красава!», переезд в `Vibe-Moments-Technologies/universal-schedule-app`
      - [x] Новый applicationId/bundleId `ru.vibemoments.krasava`
      - [x] Ротация ключа подписи (RSA-4096 из секретов CI, утёкший debug.keystore удалён)
      - [x] Все сборки (dev + release) подписываются новым ключом
      - [x] Единый GBox-источник `krasava-unified` (все каналы в одном списке)
      - [x] R3: ДОП-пары (розовый бейдж, тумблер скрытия, фильтрация в напоминаниях)
      - [x] Фикс update-checker: бета не предлагается при выключенном тумблере
      - [x] Команда: e-mail вместо описания, чипы-ссылки, отдел «Карты» (KiraSunshine)
      - [x] «О программе»: блок «Контакты» (почта + Issue) вместо «Есть проблема или идея?»
      - [x] Ключи хранилища `mirea_*` → `krasava_*`
- [x] **R1. Переработка «О программе»** — 26.2.1 ✅
      - [x] `AboutScreen` — компактная страница: иконка, версия, копирайт
            «© 2026 Vibe Moments Technologies» + «Наши права защищены ·
            Не связано с РТУ МИРЭА», easter egg на 8 тапов сохранён
      - [x] `TeamScreen` — декларативные карточки с аватарками из ресурсов
            (отделы для будущей группировки), без подгрузки из сети
      - [x] `LicensesScreen` — GPL v3, библиотеки, данные и карты, юрдокументы;
            без доменов внешних API
      - [x] «Есть проблема или идея?» перенесена в About; дубль с главной
            «Другого» убран
      - [x] Соцкнопки-иконки GitHub/Telegram/Discord/Boosty (последние —
            тост «скоро» в теме приложения)
- [x] 26.2.1-beta.1 — сборка R1: галерея сервисов без заголовка,
      социкнопки влезают над доком, тост в теме приложения
      (тег `v26.2.1-beta.1` на `485b393`; стабильный 26.2.1
      не выпускался — линия переименована в 26.3.0)
- [x] 26.2.0 — конспекты, сервисы, переработанная навигация, новая иконка,
      номера недель из маркеров iCal (+ расчёт-фолбэк от понедельника
      недели начала семестра), фиксы проверки обновлений (dev без тумблера
      беты; Android качает APK вместо 404-страницы), выбор расписания для
      уведомлений и своё время напоминаний (1–120 мин), VPN-баннер,
      юридические документы, Android privacy-hardening
      (убраны `DETECT_SCREEN_CAPTURE`, `AD_ID`, неиспользуемые модули AppMetrica)
